/*
 * ⚠️  PLACEHOLDER WORDING — NOT LEGAL COPY, NOT CONFIRMED BY LARRY.
 *
 * The terms themselves come from the user flow doc and match the answers on
 * /faq: changes or a full refund at any point up to the order entering the
 * build queue, nothing after it. This is the short form shown at the point of
 * commitment. It needs replacing with the wording that has actually been
 * checked before anything can be sold against it.
 */

export const ORDER_POLICY_HEADING = "Before you order";

export const orderPolicy: string[] = [
  "You can change your specification, or take a full refund, at any point before your order is submitted to the build queue. Submission is a separate step from payment, so there is a genuine window after you have paid.",
  "Once the order is submitted to the build queue the specification is fixed and cannot be changed — the timbers have been selected and the work has started. From that point any refund is capped at 50% of the order value, because the instrument is built to your specification and cannot simply be sold to the next person.",
];

export const POLICY_CONFIRMATION =
  "I have read and understood the return/order policy";
